export interface Stocks {
  description: string;
  symbol: string;
  d: number;
  c: number;
  o: number;
  h: number;
}

export interface IStocks {
  description: string;
  symbol: string;
}
